#include<stdio.h>
#include<conio.h>
int main()
{
    int arr[5], i,num;
    printf("Enter any 5 array elements: ");
    scanf
    for(i=0; i<5; i++)
        scanf("%d", &arr[i]);
    for(i=0; i<5; i++)
    {
        if(arr[i]%2==0)
        {
            printf("even ");
            break;
        }
    }
    getch();
    return 0;
}
